import {
	createStore
} from 'vuex'

export default createStore({
	state: {
		menuKey: 'main'
	},
	getters: {},
	mutations: {
		// changeMenu(state, key) {
		// 	state.menuKey = key;
		// },
	},
	actions: {},
	modules: {}
})